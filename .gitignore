# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*
.terragrunt-cache
generate-backend-remote.tf
terragrunt.auto.tfvars.json

# Crash log files
crash.log

# Ignore any .tfvars files that are generated automatically for each Terraform run. Most
# .tfvars files are managed as part of configuration and so should be included in
# version control.
#
# example.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json


common-local.yaml
vars-local.yaml
generated-*
.terraformrc-local
# TODO: allow .terraform.lock.hcl for more strict version control
.terraform.lock.hcl
makefile
.terraform
version.tf
versions.tf
venv
